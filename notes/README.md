# notes/

Session notes, call summaries, scratch thinking.

Keep these. The engagement log in `context/07-engagement-log.md` is the formal
record and should stay tight; this is where the messy version lives — what was
tried, what failed, what a call actually felt like.

One file per session or per call, named by date:

```
2026-09-13-claude-code-migration.md
2026-09-16-va-preapplication-conference.md
```
